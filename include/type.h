#ifndef TYPE_H
#define TYPE_H
typedef enum
{
    LOGIN,
    REGISTER,
    LOGOUT,
    CREATE_ROOM,
    JOIN_ROOM,
    GET_ROOM_LIST,
    GET_TOP_SCORE,
    MOVE,
    SURRENDER,
    OUT_ROOM
} type;
#endif // TYPE_H